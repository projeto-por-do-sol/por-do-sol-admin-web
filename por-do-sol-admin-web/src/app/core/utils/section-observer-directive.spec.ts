import { SectionObserverDirective } from './section-observer-directive';

describe('SectionObserverDirective', () => {
  it('should create an instance', () => {
    expect(new SectionObserverDirective()).toBeTruthy();
  });
});
